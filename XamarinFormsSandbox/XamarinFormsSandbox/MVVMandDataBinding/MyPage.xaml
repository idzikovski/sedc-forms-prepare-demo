<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:mvvmanddatabinding="clr-namespace:XamarinFormsSandbox.MVVMandDataBinding"
    xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
    ios:Page.UseSafeArea="True"
    x:Class="XamarinFormsSandbox.MVVMandDataBinding.MyPage">
    <ContentPage.BindingContext>
        <mvvmanddatabinding:MyViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Entry Placeholder="Enter some text here"/>
            <Label Text="{Binding LabelText}"/>

            <Slider x:Name="slider"
                    Minimum="0"
                    Maximum="100"/>
            <Label Text="{Binding Value}"
                   BindingContext="{x:Reference slider}"/>

            <Entry Placeholder="Button controlled text"
                   Text="{Binding EntryText}"/>
            <Label Text="{Binding ButtonControlledText}"/>
            <Button Text="Change text"
                    Command="{Binding TextButtonClickedCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
